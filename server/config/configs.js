"use strict";
const plugins = require('./pluginsConfig')
const routes = require("./routeConfig")
const views = require("./viewsConfig")

module.exports = {
  plugins : plugins,
  routes : routes,
  views : views
}